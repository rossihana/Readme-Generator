{
  "name": "readme-generator-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for AI Readme Generator",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix frontend\" \"uvicorn api.main:app --host 0.0.0.0 --port 8000\"",
    "build": "cd frontend && npm install && npm run build && cd .. && mkdir -p dist && cp -r frontend/dist/* dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}