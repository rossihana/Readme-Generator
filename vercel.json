{
  "builds": [
    {
      "src": "frontend",
      "use": "@vercel/static-build",
      "config": {

        "buildCommand": "npm run build",
        "installCommand": "npm install"
      }
    },
    {
      "src": "api/main.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb",
        "runtime": "python3.9"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/main.py"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}